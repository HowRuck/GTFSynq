# VCS & CI
.git
.github
.gitignore

# IDE / editor
.idea
.zed
.opencode

# Gradle build outputs & caches
.gradle
build/
**/build/
bin/
**/bin/

# Buildpacks (bootBuildImage needs full build context; only exclude outputs & local data)
docker-compose*.yaml

# Docs & metadata (not needed for the build)
README.md
SECURITY.md
LICENSE

# Local data & env
data/
.env
state_dump.bin
